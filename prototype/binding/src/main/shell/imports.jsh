import static com.geomatys.crsjson.Registry.printCRS;
import static com.geomatys.crsjson.Registry.printOperation;

// Initialization
System.out.println("GeoAPI implementation: " + com.geomatys.crsjson.Registry.INSTANCE.vendor().orElse("none found."));
